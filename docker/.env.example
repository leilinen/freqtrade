# 复制本文件为同目录下的 .env，填入实际的 user_data 绝对路径后再启动。
#   cp docker/.env.example docker/.env
# docker compose 会自动读取 docker/.env 来展开 ${USERDATA_DIR}。
#
# 该路径是宿主机上 freqtrade 仓库内的 user_data/ 目录（含策略与配置），
# 会被绑定挂载到容器 /freqtrade/user_data，便于热加载策略/配置。
#
# 必填。留空或缺失则 compose 启动会报错（避免误用错误路径）。

# 本地开发示例（macOS）：
USERDATA_DIR=/Users/lilin/Documents/lilin/code/freqtrade/user_data

# Azure 部署示例：
# USERDATA_DIR=/home/azureuser/freqtrade-develop/user_data
